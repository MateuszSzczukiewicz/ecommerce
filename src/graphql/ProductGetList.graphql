query ProductsGetList($take: Int, $skip: Int) {
	products(take: $take, skip: $skip) {
		data {
			id
			name
			description
			categories {
				name
			}
			images {
				url
				alt
				width
				height
			}
			price
		}
		meta {
			total
		}
	}
}
